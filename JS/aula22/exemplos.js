let precoProdutos = [150, 200, 250, 300, 350, 400, 450, 500, 550, 600]
let precoComDesconto = precoProdutos.map((itens) => itens * 0.7)
console.log(`preco dos produtos: ${precoProdutos}`)
console.log(`preço dos produtos com desconto: ${precoComDesconto}`)
